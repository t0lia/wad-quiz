# HW2 Images

Place stage artwork files in this directory.

Each stage in `stages/01-stages.yaml` now declares an `image` filename. Bundled HW2 consumers should resolve those filenames relative to `hw2/img/` by default.
